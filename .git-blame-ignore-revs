# Commits to skip in git blame (GitHub honours this file automatically; locally run
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# ).

# Add .gitattributes and renormalise every text file to LF. The bulk of this commit is
# line-ending churn in index/downloads/releases/contributing, brand-tokens.css and the
# brand SVGs; the handful of real edits it also carries (docs sidebar labels, legend
# placement, drawer nav aria-label) are documented in the commit message.
1b4ad1c9d913c44231f0449b7eddf7d3e5709097
